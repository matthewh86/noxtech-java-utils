autobump{
    majorPattern = ~/\[major\]|\[breaking\]/
    minorPattern = ~/\[minor\]|\[feature\]/
    patchPattern = ~/\[patch\]|\[new-snapshot\]/
    newPreReleasePattern = ~/\[new-pre-release\]|\[release-candidate\]/
    promoteToReleasePattern = ~/\[promote\]|\[release\]/
}